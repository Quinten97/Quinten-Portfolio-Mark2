import "./Project.css";

// TODO: make this componet use the props to generate a nice project display
const Project = ({ title, img, description, preview, repo }) => (
  <article>I am a project</article>
);

export default Project;
